// Removed in Sprint 2.4.
//
// This file was Sprint 2.1's placeholder waveform — deterministic fake
// bar heights with a CSS animation, used to build and test the Practice
// screen's layout before real audio capture or waveform analysis
// existed. It has been replaced outright by `WaveformVisualizer.tsx` in
// this same folder, which reads real microphone amplitude data via
// `useWaveform` and `lib/waveformSource.ts`. Nothing in the codebase
// imports this file anymore.
//
// Left in place (rather than deleted) only because this workspace's
// sandbox permissions didn't allow removing it outright; it is safe to
// delete by hand.
