// Removed in Sprint 2.2.
//
// This file was Sprint 2.1's placeholder recording state hook — a fake
// clock with no real audio capture, used only to build and test the
// Practice screen's UI before microphone access existed. It has been
// replaced outright by `useAudioRecorder.ts` in this same folder, which
// does the same job with a real MediaRecorder behind it. Nothing in the
// codebase imports this file anymore.
//
// Left in place (rather than deleted) only because this workspace's
// sandbox permissions didn't allow removing it outright; it is safe to
// delete by hand.
